{
    "rules": {
        "madrun/insert-test-dts": "on",
        "coverage/add-to-exclude": ["on", {
            "list": ["**/*.ts"]
        }]
    },
    "match": {
        "{lib,bin,test}": {
            "madrun": "off"
        }
    }
}
